#!/usr/bin/env bash

set -euo pipefail

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
context="${EDGE16_SERENA_CONTEXT:-codex}"

exec serena start-mcp-server \
  --project "${repo_root}" \
  --context="${context}" \
  --open-web-dashboard=false \
  "$@"
